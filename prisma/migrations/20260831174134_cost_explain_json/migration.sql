BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[rfq_cost_summaries] ADD [explain_json] NVARCHAR(max);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
